function checkProfile() {
  var check

  check.name = postMessage
  check.password = postMessage

  if (check.name && check.spec == true) {
    return ValidityState
  } else {
    return clearInterval
  }
}

export default checkProfile

// This code is wrong complety ... 🤣